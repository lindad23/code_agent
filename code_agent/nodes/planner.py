from __future__ import annotations

from code_agent.state import CodeAgentState


def planner(state: CodeAgentState) -> dict:
    """TODO(next): Plan richer debugging steps before running nodes."""
    return {}
