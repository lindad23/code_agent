# Requires Python 3.11 (recommended) and Git installed on the system.
# Installs this project in editable mode with development and experiment extras.
-e .[dev,experiment]

# The main development environment may use CPU PyTorch. Experiment runs select
# and verify a host-compatible PyTorch runtime from nvidia-smi/driver detection.
# Do not install plain `torch` here: default PyPI wheels may target a newer CUDA
# runtime than the host NVIDIA driver supports.
